name: Push to main
on:
  push:
    branches:
      - '*'
jobs:
  push-to-main:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Push to main
        run: git push origin HEAD:main --force
